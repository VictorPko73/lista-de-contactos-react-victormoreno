import { AGENDA_SLUG } from "../services/agendaService";
export const Footer = () => (
	<footer className="footer mt-auto py-3 text-center">
		<p>
			Contact list for {AGENDA_SLUG}
		</p>
		
	</footer>
);